# =============================================================================
# Homelab Dashboard Environment Configuration
# =============================================================================
# Copy this file to .env and update with your values
# This single .env file is used by both frontend and backend

# =============================================================================
# Security (REQUIRED)
# =============================================================================

# JWT Secret for authentication (REQUIRED)
# Generate with: openssl rand -base64 32
# SECURITY: Must be a strong, random string of at least 32 characters
JWT_SECRET=your-secure-random-jwt-secret-here-change-this

# JWT Token Expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Encryption Configuration (REQUIRED)
# CRITICAL: Generate a 32-byte encryption key for AES-256
# Generate with: openssl rand -base64 32
# This key encrypts sensitive credentials (API keys, passwords) in the database
ENCRYPTION_KEY=your-encryption-key-change-in-production-must-be-32-bytes

# =============================================================================
# Server Configuration
# =============================================================================

# Port to expose the application on
# Development: 3000, Docker/Production: 3350
PORT=3000

# Node environment (development, production)
NODE_ENV=development

# =============================================================================
# Database
# =============================================================================

# SQLite database path
# Development: ./dash.db (relative to backend directory)
# Docker: Handled by Docker volumes
DATABASE_PATH=./dash.db

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed CORS origins
# Development: Include Angular dev server
# Production/Docker: Use the external URL where you'll access the app
# Example: http://192.168.1.100:3350,https://dashboard.example.com
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3350

# =============================================================================
# Default Admin User (Initial Setup)
# =============================================================================

# Default admin credentials for initial database seeding
# If you lose the password, just re-run the seed script or re-deploy
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_DISPLAY_NAME=Administrator

# =============================================================================
# External APIs (Optional)
# =============================================================================

# Pexels API key for background images
PEXELS_API_KEY=your-pexels-api-key

# =============================================================================
# Upload Configuration
# =============================================================================

# Maximum upload size in bytes (default: 10MB)
UPLOAD_MAX_SIZE=10485760

# =============================================================================
# System Monitoring (Optional)
# =============================================================================

# Enable Docker host system monitoring
# Set to true to monitor the Docker host instead of the container
# Requires mounting /proc and /sys volumes (see docker-compose.yml)
MONITOR_DOCKER_HOST=false

# =============================================================================
# Additional Notes
# =============================================================================
# - Database is SQLite and stored in backend/dash.db (development) or Docker volume (production)
# - Uploads are persisted in backend/uploads (development) or Docker volume (production)
# - WebSocket is available at ws://localhost:3000/ws (development) or ws://localhost:3350/ws (production)
# - Health check endpoint: http://localhost:3000/health or http://localhost:3350/health
