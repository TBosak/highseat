# Highseat Docker Environment Configuration
# Copy this file to .env and update with your values

# =============================================================================
# Security (REQUIRED)
# =============================================================================

# JWT Secret for authentication (REQUIRED)
# Generate with: openssl rand -base64 32
# SECURITY: Must be a strong, random string of at least 32 characters
JWT_SECRET=your-secure-random-jwt-secret-here-change-this

# =============================================================================
# Server Configuration
# =============================================================================

# Port to expose the application on
PORT=3350

# Node environment
NODE_ENV=production

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed CORS origins
# For Docker, use the external URL where you'll access the app
# Example: http://192.168.1.100:3350,https://highseat.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3350

# =============================================================================
# Upload Configuration
# =============================================================================

# Maximum upload size in bytes (default: 10MB)
UPLOAD_MAX_SIZE=10485760

# =============================================================================
# System Monitoring (Optional)
# =============================================================================

# Enable Docker host system monitoring
# Set to true to monitor the Docker host instead of the container
# Requires mounting /proc and /sys volumes (see docker-compose.yml)
MONITOR_DOCKER_HOST=false

# =============================================================================
# Database (Handled by Docker volumes)
# =============================================================================
# Database is SQLite and stored in Docker volume 'highseat-db'
# No additional configuration needed

# =============================================================================
# Additional Notes
# =============================================================================
# - Uploads are persisted in Docker volume 'highseat-uploads'
# - WebSocket is available at ws://localhost:3350/ws
# - Health check endpoint: http://localhost:3350/health
